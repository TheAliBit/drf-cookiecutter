# Default language/locale for the project (Persian - Iran)
LANGUAGE_CODE = 'fa-ir'

# Server-side timezone used by Django internally (recommended to keep UTC)
TIME_ZONE = 'UTC'

# Enable Django's internationalization system (translation, formatting)
USE_I18N = True

# Enable timezone-aware datetimes across the project
USE_TZ = True
